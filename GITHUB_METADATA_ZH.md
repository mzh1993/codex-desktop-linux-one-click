# GitHub 仓库元信息建议

这份文档用于你创建远端仓库页面时直接复制粘贴。

## 推荐仓库名

首选：

- `codex-desktop-linux-one-click`

备选：

- `codex-desktop-ubuntu20`
- `codex-desktop-linux-compat`

## About / 简介

英文版：

> One-click Codex Desktop for Ubuntu 20.04 and older Linux hosts, without upgrading the kernel or replacing the system compiler.

中文版：

> 面向 Ubuntu 20.04/老 Linux 的 Codex Desktop 一键解决方案，不升级内核，不替换系统默认编译器。

## GitHub Description

建议直接使用：

```text
One-click Codex Desktop for Ubuntu 20.04 and older Linux hosts, with Docker-based native rebuild, glibc-compatible runtime fixes, Linux black-screen patch, and desktop integration.
```

## Topics

建议加这些 topic：

- `codex`
- `openai`
- `electron`
- `linux`
- `ubuntu`
- `ubuntu-20-04`
- `docker`
- `desktop`
- `compatibility`
- `one-click`

## 仓库置顶卖点

建议在 README 开头或置顶说明里突出这几个点：

- Ubuntu 20.04 compatible
- no kernel upgrade required
- no system compiler replacement required
- Docker-based rebuild path
- glibc-compatible runtime output
- Linux black-screen fix
- GNOME launcher and favorites support

## 社交分享一句话

英文：

> Finally got Codex Desktop working on Ubuntu 20.04 and older Linux without upgrading the kernel or replacing the system compiler. This repo packages the whole workaround into a one-click flow.

中文：

> 终于把 Codex Desktop 在 Ubuntu 20.04/老 Linux 上跑通了，不需要升级内核，也不需要替换系统默认编译器。这个仓库把整套兼容方案整理成了一键流程。
